#Compile TP6 from the source code

##Installation
Instller reports 

```
Too many subdirectories:  Press ESC.
```

Turns out the installer just doesn't like any subdirectories in
the distribution. Just remove them and it will work just fine.
 